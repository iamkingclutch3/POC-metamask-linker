import "colors";
export class TextDatabase {
    private options;
    private _file;
    constructor(options: {
        /**
         * The name of the database
         */
        databaseName: string;
        /**
         * The relative path to store the data
         */
        location: string;
    });
    writeData(key: string, data: Array<string> | string | number | boolean): void;
    getData(key: string): string | number | boolean | {
        [key: string]: any;
    } | undefined;
    private logError;
}
